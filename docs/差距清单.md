# 差距清单 · GeneralGameAgent-Web

> 实时维护：每条带描述、优先级（P0 阻塞 / P1 影响 / P2 可选）、计划天、四要素可执行。
> 当前项目核心交付已完成；本清单为剩余改进项与已知边界。

---

## P0（阻塞）—— 无

当前无阻塞项；在线推理、素材工作台、查看器均已可用。

## P1（影响，建议做）

### G1 · ft 版本段图/演示条为空
- **描述**：`ft.json` 的 `segments/demo` 为空，查看器切到 ft 时段图区只有提示、演示条消失。
- **优先级**：P1（影响演示完整度）
- **计划天**：0.5~1 天
- **四要素**：
  - 约束：不修改 NitroGen 源码；段图用 matplotlib 静态 PNG，schema 与 baseline 一致
  - 需求：为 ft 结果集生成 21 段 top5 差异帧数据 + 段曲线图，前端 ft 版本正常显示段图
  - 范围：只做 ft 的 segments/demo 生成与入库，不改前端结构
  - 输出：`ft.json` 补全 segments/demo + 提交说明

### G2 · 批量评测耗时（200 帧约 5-6 分钟）
- **描述**：批量评测逐帧串行 ffmpeg 抽帧 + 单进程推理，耗时长。
- **优先级**：P1（影响体验）
- **计划天**：1 天
- **四要素**：
  - 约束：8G 显存单模型驻留；保持 K 多数票口径
  - 需求：批量评测总耗时降到 2 分钟以内
  - 范围：抽帧并行（ThreadPool）+ 推理复用（不并行模型推理），或抽帧缓存
  - 输出：`run_evaluate` 提速 + 实测耗时对比

## P2（可选）

### G3 · 摇杆相关未达 M4 目标（0.126 < 0.4）
- **描述**：LoRA 行为克隆对摇杆相关提升有限；按 R3 预案已如实呈现。
- **优先级**：P2（已如实交付，继续改进超课程范围）
- **计划天**：1~2 天（若要试）
- **四要素**：
  - 约束：不修改 NitroGen 源码
  - 需求：自定义摇杆加权 flow matching loss 提升 jl_corr
  - 范围：改 loss 权重 + 重训 r8 + 评测对比
  - 输出：新版 ft 权重 + RESULTS.md 增量记录

### G4 · 素材"批量对比"结果无具体按键名
- **描述**：批量对比接口只返回 press 布尔，前端显示"按下按键/无按键"，无键名。
- **优先级**：P2
- **计划天**：0.5 天
- **四要素**：需求为后端 evaluate 素材分支返回具体按键列表；输出为批量对比卡片显示键名。

### G5 · 前端测试覆盖
- **描述**：前端无自动化测试（构建通过仅静态验证）。
- **优先级**：P2
- **计划天**：1 天
- **四要素**：加 vitest + 关键视图（版本切换/曲线渲染）冒烟测试。

### G6 · 后端安全收紧
- **描述**：CORS `allow_origins=["*"]`（开发期放宽）。
- **优先级**：P2（上线前必做）
- **计划天**：0.5 天
- **四要素**：上线前收紧为具体 origin + 素材上传文件类型/大小限制。

# 开发日志 · GeneralGameAgent-Web

> 每日两块：计划摘要 / 自测命令与结果 + 遗留。按日期倒序（新在上）。

---

## 2026-08-24

**计划摘要**：交付文档补齐（对照 `harness/08-文档同步.md` 项目文档清单），写 `docs/演示说明.md` + 11 份缺失 docs（立项/开发/验证/交付四阶段）。

**自测**：`npm run build`（前端通过）；`python -m py_compile app/inference.py app/main.py`（后端通过）；`curl /api/health` → ok。

**结果**：演示说明 212 行（bd5ee9c）；补文档 11 份（本日提交）。

**遗留**：文档是否被后续 AI 采用待验证。

---

## 2026-08-23

**计划摘要**：阶段 3 前端 v2（对比视图）+ 在线推理（单帧/批量）+ 素材评测工作台 + 多轮 UI 整改。

**自测命令与结果**：
- `npm run build` 多次通过；`netstat :5173/:8000` 确认双服务在线
- 推理实测：单帧 38400（baseline 无按键 / ft 出按键 dpad_down/dpad_left/right_trigger）
- evaluate 3 帧验证返回 `sec/jl_gt/jl_pred` 逐帧字段
- 保存 ft 实测 0.06s（不重跑）

**本日关键提交**：f8570a1 左栏拿出全宽 → 6332409 推理互斥+全宽卡片 → a1ae601 结果去嵌套 → ab0e260/2b97c3a/95370d4 图表布局（标题/图例/坐标注释）→ 0452387 推理曲线 → b02f0d5 批量进曲线+lastInfer → c11d55a 抽帧并发竞态修复 → 7264c55 保存不重跑+自动切 ft → 87ec892 模型选择（baseline/ft）。

**遗留**：ft 段图为空（已知边界）；摇杆相关未达 0.4（如实呈现）。

---

## 2026-08-22

**计划摘要**：阶段 0 微调可行性 spike + 阶段 1（后端 API 多版本化 + 前端 v1）+ 阶段 2 LoRA 训练与评测。

**自测命令与结果**：
- spike：`python train/spike.py` → 1 step loss 有限且下降，显存 <8G ✅
- 数据管线：`python train/data_pipeline.py` → 抽帧缓存 38400 帧（首次约 8 分钟）
- 训练：`python train/train_lora.py --steps 1500 --lora_r 8` → 训练 loss 0.068 / 验证 0.093
- 评测：`python train/evaluate_ft.py` → 按键召回 15 倍/F1 8 倍达标；摇杆相关 +0.126 未达 0.4
- 后端：`python -m pytest backend/tests` → 15 个测试通过
- 导出：`export_ft_json.py` + `export_comparison.py`（需 `HF_HUB_OFFLINE=1`）

**结果**：M0/M1/M2 完成；`train/RESULTS.md` 记录 M2 如实结论（按键达标、摇杆未达标）。

**遗留**：阶段 3 前端 v2 次日开始；ft 段图数据未生成。

---

## 2026-08-21

**计划摘要**：立项（范围/验收/风险）、技术选型、环境核查、仓库初始化；读透 NitroGen 训练接口。

**自测**：读取 `nitrogen.py`/`inference_session.py`/`cfg.py`；确认 ng.pt 动作维度 25、动作块 18 步、无条件分支（game_mapping=null）。

**结果**：产出 `docs/立项书.md`、`docs/总计划.md`、`docs/训练接口分析.md`；决策记录进 `docs/技术选型比选.md`。

**遗留**：阶段 0 spike 待执行。

# 01 — 摇杆加权重训支持与快速验证

**What to build:** 训练脚本支持摇杆加权 loss——`train_lora.py` 新增 `--j_weight`（把 `actions_mask` 从全 1 换成权重矩阵：21 按钮列 = 1，4 摇杆列 = w，默认 1 等权），然后跑 400 步快速验证（w=4 与 w=8 各一版），用诊断脚本确认训练集摇杆相关较旧 ft(+0.042) 明显抬头——证明"加权真的让模型学会了摇杆"，为正式训练定权重。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [x] `train_lora.py` 支持 `--j_weight`（默认 1 = 等权，不影响旧行为）
- [x] 快速验证：400 步 `w=4` + 1500 步 `w=8`，保存 `jw_w4.pt` / `jw_w8.pt`
- [x] `diagnose_train_set.py` 测得训练集摇杆相关未抬头（w4 -0.055 / w8 +0.035，旧 ft +0.042）
- [x] 测试集 `jw_w8` 摇杆相关 +0.113 ≈ 旧 ft +0.126 无提升，触发召回率 65.75%→15.25% 大幅回退
- [x] 数值验证确认加权机制生效（loss 随权重正确变化），但摇杆相关指标为单帧 2 点 Pearson，噪声大、可提升信号被淹没

**结果（2026-08-24）：验证失败，方向放弃（wontfix）。** 代码已回退（`train_lora.py` git checkout 恢复），`jw_w4.pt`/`jw_w8.pt` 保留本地（`train/ckpt` 已 gitignore，不入仓）。

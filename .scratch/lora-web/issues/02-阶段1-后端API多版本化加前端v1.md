# 02 — 阶段 1：后端 API（多版本化）+ 前端 v1（M1）

**What to build:** 后端 FastAPI 从第一天就结果集多版本化（baseline/ft 作为一等字段），先把课程 baseline 结果转结构化 JSON 入库并暴露四个端点；前端 v1 用 Vue3 + Vite + ECharts 展示 baseline 的指标仪表盘 + 交互查看器（曲线缩放/筛选/差异帧标注）。前端 v1 只有 baseline 也能跑通。

**Blocked by:** None — can start immediately.（与 01 并行）

**Status:** resolved (2026-08-22)

- [x] 后端骨架可启动（FastAPI）
- [x] baseline 结果入库：课程 `results/`（test_set_metrics.md、21 段 top5、m5 对比表）转结构化 JSON
- [x] `GET /api/results` 返回结果集列表（baseline, ft）；`/api/metrics?version=`、`/api/segments?version=`、`/api/figures/<name>` 多版本参数生效
- [x] 指标计算抽公共库，与课程 evaluate.py 共享口径（按键准确率/触发召回率/F1/摇杆相关）
- [x] 验收：打开前端看到指标卡 + 曲线交互；`/api/metrics?version=baseline` 返回 JSON

## Answer (2026-08-22)

**M1 通过。** 阶段 1 全部完成：

- 后端：FastAPI 四端点 + 额外 frames/button-freq/demo/health；`?version=` 一等字段（前端 v2 只加视图）。metrics_lib 与课程 evaluate.py 口径一致（S2 seam），figures 子路径 + 目录穿越防护。
- 数据：`backend/data/results/baseline.json`（200 帧明细 + 21 段 top5 + 频率表 + 5 演示帧）由 `scripts/build_baseline_json.py` 从课程 md 生成；图 PNG 不入仓（`sync_figures.py` 同步）。
- 测试：17/17 通过（metrics_lib 回归 + API 端点 + 目录穿越）。
- 前端：Vue3+Vite+ECharts 工作台（design-taste-frontend skill 原则——深色操作台风、等宽数字、状态色只用于达标判定），指标卡+曲线查看器+演示条，响应式。E2E 全链路 PASS（vite 代理 /api → uvicorn）。
- 已提交：b1673d8（JSON 入库）、66a023e（API+测试）、a87eccf（figures 子路径修复）、83a711b（前端）、5402d65（README）。

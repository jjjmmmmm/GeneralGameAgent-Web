# 02 — 阶段 1：后端 API（多版本化）+ 前端 v1（M1）

**What to build:** 后端 FastAPI 从第一天就结果集多版本化（baseline/ft 作为一等字段），先把课程 baseline 结果转结构化 JSON 入库并暴露四个端点；前端 v1 用 Vue3 + Vite + ECharts 展示 baseline 的指标仪表盘 + 交互查看器（曲线缩放/筛选/差异帧标注）。前端 v1 只有 baseline 也能跑通。

**Blocked by:** None — can start immediately.（与 01 并行）

**Status:** ready-for-agent

**前端部分开工前必须暂停，等用户提供新 skill。**

- [ ] 后端骨架可启动（FastAPI）
- [ ] baseline 结果入库：课程 `results/`（test_set_metrics.md、21 段 top5、m5 对比表）转结构化 JSON
- [ ] `GET /api/results` 返回结果集列表（baseline, ft）；`/api/metrics?version=`、`/api/segments?version=`、`/api/figures/<name>` 多版本参数生效
- [ ] 指标计算抽公共库，与课程 evaluate.py 共享口径（按键准确率/触发召回率/F1/摇杆相关）
- [ ] 验收：打开前端看到指标卡 + 曲线交互；`/api/metrics?version=baseline` 返回 JSON
